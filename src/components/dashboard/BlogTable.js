import React from 'react';
const BlogTable = ({blog}) => {
    console.log(blog)
    return (
        <div className=''>
            <h1>Here we want to show our all blogs</h1>
        </div>
    );
};

export default BlogTable;